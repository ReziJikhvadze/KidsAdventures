frontend-dist/
frontend-deploy.zip
_frontend-deploy-staging/
_build_check/
publish/
publish2/
publish-test/
publish-test2/
publish-verify/
publish.zip

# Real credentials live here. Ship them through App Service configuration or a
# key vault — appsettings.Production.example.json is the template that stays tracked.
appsettings.Production.json
