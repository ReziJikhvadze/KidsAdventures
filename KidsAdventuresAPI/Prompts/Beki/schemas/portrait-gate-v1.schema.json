{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "portrait-gate-v1",
  "title": "Beki Portrait Gate v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["accepted", "reason", "explanation"],
  "properties": {
    "accepted": {
      "type": "boolean",
      "description": "True whenever a photographed person is present in the image."
    },
    "reason": {
      "type": "string",
      "description": "One code. 'ok' when accepted; 'not_a_person' when the image contains no photographed person.",
      "enum": ["ok", "not_a_person"]
    },
    "explanation": {
      "type": "string",
      "description": "One short English sentence describing the image, for the product log only.",
      "maxLength": 300
    }
  }
}
