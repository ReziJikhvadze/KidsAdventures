import type { ThemeType } from "@/lib/api/types";

export const THEME_TINTS: Record<ThemeType, string> = {
  Airplanes: "var(--sky-soft)",
  Dinosaurs: "var(--mint)",
  Space: "var(--accent)",
  Pirates: "var(--sun)",
  Animals: "var(--sun)",
};
